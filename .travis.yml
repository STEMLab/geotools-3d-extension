cache:
  directories:
    - "$HOME/.m2"
    
language: java

script:
  - mvn -B -fae -Djava.awt.headless=true -Dall clean install
  
notifications:
  email: false
  on_failure: never
  
before_install:
  - ./travis/${TRAVIS_OS_NAME}/before_install.sh

jdk: 
  - oraclejdk8
