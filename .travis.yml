sudo: false

cache:
  directories:
    - "$HOME/.m2"

os:
  - linux

language: java

script:
  - mvn -B -fae -Djava.awt.headless=true -Dall clean install
  
notifications:
  email: false
  on_failure: never
  
env:

before_install:
  - ./travis/linux/before_install.sh

jdk: 
  - oraclejdk8
